import React from "react";

const Contacts = () => {
    return (
        <div className="contacts" id="contacts">
            <h1>Contacts Page</h1>
        </div>
    );
};

export default Contacts;
